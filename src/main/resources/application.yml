spring:
  profiles:
    active: local
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

server:
  port: 8080

aws:
  region: ap-northeast-2
  s3:
    bucket: dogcument-image